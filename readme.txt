behavior: swap(list) takes two values in a list and returns the values swapped
example: swap([1,3]) returns [3,1]
extra credit constraints: copying the list or creating a new one not allowed! alter it in place, and then return it